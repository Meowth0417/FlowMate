# FlowMate

面向研发协作的任务流转系统：让一个需求从创建、理解、设计、前后端并行开发、验证、审查到交付沉淀，在统一流程中完成流转、协作、审批与产物沉淀。平台负责编排流程与留存产物，agent 在执行用户本地环境运行。

## Language

### 用户与角色

**系统角色**:
用户在系统层面的唯一身份，取值为 产品经理、开发、观察者。每个用户仅有一个系统角色。
_Avoid_: 权限组、用户组

**负责人字段**:
任务创建时确定的任务级职责绑定：创建人、需求负责人、前端开发、后端开发、产品经理。与系统角色不同，属于单个任务的配置，创建后不可修改。
_Avoid_: 任务角色、成员

**需求负责人**:
推动任务主流程的开发用户，默认等于创建人。
_Avoid_: 负责人、owner

**观察者**:
只读用户，可查看全部任务与产物，但不可创建、执行、确认、补充。
_Avoid_: 访客、游客

**相关用户**:
某任务的创建人、需求负责人、产品经理、前端开发、后端开发之一。决定非观察者用户能否看到该任务。
_Avoid_: 参与者、成员

### 流程与阶段

**阶段**:
主流程的一个环节。共享阶段（需求理解、详细设计、交付沉淀）全任务唯一；分支阶段（开发、功能验证、代码审查）在前端与后端各存在一份。
_Avoid_: 步骤、节点、环节

**分支**:
阶段所属的执行线，取值 shared / frontend / backend。前后端分支从开发阶段起并行推进、各自独立通过。
_Avoid_: 支线、通道

**执行**:
在某阶段发起一次 agent 运行，产出该阶段产物。对应权限编码的第 1 位。
_Avoid_: 运行、跑一次

**确认**:
对阶段结果（尤其澄清问题答案、审批通过）的人工拍板。对应权限编码的第 2 位。
_Avoid_: 审批、批准

**补充**:
向阶段追加提示词或需求/设计内容并生成新版本。对应权限编码的第 3 位。
_Avoid_: 追加、补录

**权限编码**:
三位 `[执行][确认][补充]` 编码，1 有权限、0 无权限（如 `111`、`101`）。
_Avoid_: 权限位、mask

**附加提示词**:
阶段执行前用户填写的 prompt，叠加在系统默认提示词之上，不覆盖默认。
_Avoid_: 自定义 prompt、覆盖提示词

### 需求理解

**澄清问题**:
需求理解阶段产出的待确认问题，每题提供 3 个候选答案（第 1 个为推荐）加 1 个自由填写入口，仅产品经理可确认，确认后不可修改。
_Avoid_: 疑问、问题清单

**完整需求文档**:
由「原始需求材料 + 已确认问题答案 + 后续补充需求」拼接生成的正式需求输入。
_Avoid_: 需求稿、PRD

### 验证与审查

**驳回**:
功能验证不通过，固定回到本分支开发阶段，必须填理由，理由作为下次开发输入。
_Avoid_: 打回、退回

**回流**:
代码审查后按风险分级（高/中/低）选择范围，固定返回本分支开发阶段修复；分级仅用于筛选展示，不自动阻断。
_Avoid_: 返工、回退

### 执行过程

**执行过程**:
agent 运行时的实时事件流（思考 / 工具调用 / 文本），仅当前执行用户在执行中的阶段可见，阶段结束后不再展示，不提供历史回看。
_Avoid_: 运行日志、执行记录

**产物**:
阶段沉淀下来的结构化结果（如需求摘要、接口文档、变更文件清单、审查问题明细），对相关用户长期可见。
_Avoid_: 输出、结果
